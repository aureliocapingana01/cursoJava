import { MultipicarPorPipe } from './multipicar-por.pipe';

describe('MultipicarPorPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipicarPorPipe();
    expect(pipe).toBeTruthy();
  });
});
